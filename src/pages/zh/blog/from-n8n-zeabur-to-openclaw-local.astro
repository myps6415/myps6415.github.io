---
import BlogPost from "../../../layouts/BlogPost.astro";
import StatRow from "../../../components/blog/StatRow.astro";
import Callout from "../../../components/blog/Callout.astro";
import DiffBlock from "../../../components/blog/DiffBlock.astro";
import Lesson from "../../../components/blog/Lesson.astro";
import CodeBlock from "../../../components/blog/CodeBlock.astro";
import { posts } from "../../../data/posts";

const post = posts.find((p) => p.slug === "from-n8n-zeabur-to-openclaw-local")!;
const loc = post.zh;
---

<BlogPost
  lang="zh"
  slug={post.slug}
  title={loc.title}
  description={loc.description}
  tag={loc.tag}
  date={post.date}
  meta={[{ text: "myps6415" }]}
>
  <StatRow
    stats={[
      { value: "$7.88 → $0", label: "每月費用" },
      { value: "10 → 1", label: "n8n 節點 → Python 檔" },
      { value: "+1", label: "順手修掉的潛在 bug" },
    ]}
  />

  <h2>背景</h2>

  <p>
    過去一年，我在 Zeabur 上養著一個 n8n workflow：
    <code>family_schedule_alert</code>。每天早上 6:00 抓 Google Calendar 當日的事件，
    篩出 summary 含有<strong>家人名字</strong>的，整理成一段易讀的
    內容，推到一個照護群成員都在的 LINE 群組。
  </p>

  <p>
    當初選 n8n 是阻力最小的路：視覺化編輯器、內建 Google Calendar OAuth、
    Schedule trigger、HTTP node 打 LINE Push。十個 node、收工。Zeabur 跑容器、
    每月帳單寄來，6 點機器人準時報到。月費高峰是
    <strong>USD $7.88</strong>。
  </p>

  <h2>觸發點</h2>

  <p>
    2026 年中，我為了完全不相關的理由，在家裡的機器上跑了 OpenClaw —— 長時間
    執行的 agent session、Telegram bot、memory store。OpenClaw 把 cron 子系統
    做完整之後，jobs registry 長得很快：gateway 自我修復、記憶備份、內容
    生產線、Threads token 自動續期、每週投資研究。我數一下，本地已經有
    <strong>二十個 cron job</strong>，而 Zeabur 上只剩一個 n8n workflow 在
    燒每月 $7.88。
  </p>

  <p>一個 workflow 不該值一條 $7.88 的月費 line item。搬。</p>

  <h2>Case study：家人每日行程提醒</h2>

  <p>原本的 n8n graph 有十個 node：</p>

  <ul>
    <li><code>Schedule Trigger</code> — 06:00 觸發</li>
    <li>
      <code>Google Calendar</code> — 抓 <code>$today</code> 到{" "}
      <code>$today.plus(1, "day")</code> 之間的所有事件
    </li>
    <li>
      <code>If_keyword_in_summary</code> — 用{" "}
      <code>summary contains &lt;家人名&gt;</code> 篩
    </li>
    <li><code>daily_schedule_list</code> — Set node，投影欄位</li>
    <li>
      <code>if_location_undefined</code> + <code>add_location</code> +{" "}
      <code>Merge</code> — 沒有地點就補「未標示」
    </li>
    <li><code>Aggregate</code> — 把所有事件收成一個 payload</li>
    <li>
      <code>Code</code> — Python code node，格式化成一段 LINE 友善的文字
    </li>
    <li><code>line_push</code> — HTTP POST 打 LINE Push API</li>
  </ul>

  <p>
    視覺化 graph 截圖很漂亮，但每個 node 實際上都只是包了兩三行真實邏輯的
    薄殼，外加散在六七個 JSON 設定面板裡的 n8n 專屬 expression 語法。
    <em>真正的</em> 程式碼住在 Python Code node 裡 —— 視覺抽象到那邊就放棄了，
    剩下你還是要手寫字串模板拼 Python。
  </p>

  <h2>OpenClaw 重寫版</h2>

  <p>
    一個檔案、178 行、純 Python stdlib —— 沒有 n8n、沒有{" "}
    <code>node_modules</code>、沒有第三方套件。實際在做的三件事是 token
    refresh、calendar fetch、LINE push：
  </p>

  <CodeBlock
    label="~/.openclaw/scripts/family_schedule_alert.py — 關鍵段落"
    lang="python"
  >
{`def get_access_token():
    with open(TOKEN_PATH) as f:
        creds = json.load(f)
    body = urllib.parse.urlencode({
        "client_id": creds["client_id"],
        "client_secret": creds["client_secret"],
        "refresh_token": creds["refresh_token"],
        "grant_type": "refresh_token",
    }).encode("utf-8")
    req = urllib.request.Request(
        creds["token_uri"], data=body,
        headers={"Content-Type": "application/x-www-form-urlencoded"},
    )
    with urllib.request.urlopen(req, timeout=30) as resp:
        return json.loads(resp.read().decode("utf-8"))["access_token"]


def fetch_today_events(access_token):
    now_tw = dt.datetime.now(TZ)
    today_start = now_tw.replace(hour=0, minute=0, second=0, microsecond=0)
    today_end = today_start + dt.timedelta(days=1)
    params = {
        "timeMin": today_start.isoformat(),
        "timeMax": today_end.isoformat(),
        "q": KEYWORD,           # server-side prefilter
        "singleEvents": "true",
        "orderBy": "startTime",
    }
    url = (f"https://www.googleapis.com/calendar/v3/calendars/"
           f"{urllib.parse.quote(CALENDAR_ID)}/events"
           f"?{urllib.parse.urlencode(params)}")
    req = urllib.request.Request(
        url, headers={"Authorization": f"Bearer {access_token}"}
    )
    with urllib.request.urlopen(req, timeout=30) as resp:
        return json.loads(resp.read().decode("utf-8")).get("items", [])`}
  </CodeBlock>

  <p>
    OAuth 拆成兩支腳本。<code>setup_google_oauth.py</code>{" "}
    跑一次：開瀏覽器、用本機 loopback 接 auth code、換 refresh token、
    把結果寫到{" "}
    <code>~/.openclaw/credentials/google_oauth_token.json</code>{" "}
    （權限 <code>0600</code>）。之後每天的腳本只用 refresh token
    （不會過期），每次跑都換一個新的 access token。
  </p>

  <h2>順手修掉的潛在 bug</h2>

  <p>原本 n8n Set node 是這樣投影時間欄位的：</p>

  <DiffBlock
    header="格式化事件的起 / 迄時間"
    lines={[
      { kind: "del", text: "- date  = $json.start.dateTime.toDateTime().format('yyyy-MM-dd')" },
      { kind: "del", text: "- start = $json.start.dateTime.toDateTime().format('HH:mm:ss')" },
      { kind: "del", text: "- end   = $json.end.dateTime.toDateTime().format('HH:mm:ss')" },
      { kind: "ctx", text: "" },
      { kind: "add", text: "+ if 'dateTime' in start:" },
      { kind: "add", text: "+     start_dt = dt.datetime.fromisoformat(start['dateTime']).astimezone(TZ)" },
      { kind: "add", text: "+     date_s  = start_dt.strftime('%Y-%m-%d')" },
      { kind: "add", text: "+     start_s = start_dt.strftime('%H:%M:%S')" },
      { kind: "add", text: "+     end_s   = ..." },
      { kind: "add", text: "+ else:  # all-day event" },
      { kind: "add", text: "+     date_s = start.get('date', '')" },
      { kind: "add", text: "+     start_s = end_s = '全日'" },
    ]}
  />

  <p>
    Google Calendar 對全日事件回傳的是 <code>start.date</code>，不是{" "}
    <code>start.dateTime</code>。n8n 版本在第一次有人把家人的全日事件加進
    行事曆的當下就會炸。它從來沒炸 —— 是因為實務上還沒人這樣加而已。
    重寫的時候順手就修了，這種「閱讀時發現、不是 06:01 在 debug 時發現」的
    修法是最好的修法。
  </p>

  <h2>OpenClaw cron + agentTurn</h2>

  <p>
    OpenClaw 的 cron 不是 <code>crontab</code> 的薄殼。每個 job 的 payload
    是 <strong>agentTurn</strong> —— 排程到時喚醒一個 agent、丟一段 prompt
    給它、讓 agent 去做、最後把 agent 的回覆當成這次 run 的 summary。這個
    job 的 prompt 故意寫得很直白：
  </p>

  <CodeBlock
    label="cron job 86ce0520-…-6cba6f payload"
    lang="json"
  >
{`{
  "id": "86ce0520-afc8-47c7-a47f-7681fc6cba6f",
  "name": "每日行程提醒",
  "schedule": { "kind": "cron", "expr": "0 6 * * *", "tz": "Asia/Taipei" },
  "agentId": "main",
  "payload": {
    "kind": "agentTurn",
    "message": "Run this bash command and report nothing else: /usr/bin/python3 /Users/fish/.openclaw/scripts/family_schedule_alert.py",
    "timeoutSeconds": 60
  },
  "delivery": { "mode": "announce", "channel": "telegram", "to": "971742907" }
}`}
  </CodeBlock>

  <p>
    用 LLM agent 去執行一行 bash 確實有點殺雞用牛刀 —— 一次 fire 用掉 290
    input / 145 output token 也不是免費的。但同一個排程框架除了跑這種笨
    cron job，也跑那種 payload 寫著
    <em>「整理今天 Threads 的留言、有需要我注意的就跟我說」</em>{" "}
    的 reasoning job。一個 cron 介面、一套 delivery、一個歷史檢視器。
    讓 agent 去執行簡單情境的成本，就是<em>不必</em>另外跑第二套排程系統的代價。
  </p>

  <Callout variant="info" title="兩條 delivery、一個 job">
    Python 腳本把當日行程推到 <strong>LINE</strong> 群組 —— 給照護群成員看。
    OpenClaw 的 cron delivery channel 另外 announce 到我的{" "}
    <strong>Telegram</strong>：「job 跑完、status ok、耗時 11.9 秒、
    summary：今日無相關行程，跳過推播」。給使用者看的通知跟
    給維運看的通知是兩條獨立通道。n8n 版只有第一條 —— 跑壞了我可能要
    過幾天有人問「今天怎麼沒收到提醒」才會知道。
  </Callout>

  <h2>這台機器上 OpenClaw 還在做什麼</h2>

  <p>
    搬一個 workflow 本身不真的值 $7.88；價值是把搬遷成本攤平在同一台機器
    上已經在跑的其他事情上。同一個 OpenClaw cron registry 還在跑：
  </p>

  <ul>
    <li>
      <strong>排程化的內容生產線</strong> —— 數條主題垂直的多段式
      流程，每條各有自己的節奏。
    </li>
    <li>
      <strong>社群平台維運</strong> —— 排程化的內容整理、報告，
      與免人工的憑證續期。
    </li>
    <li>
      <strong>市場 / 研究</strong> —— 各自節奏的市場掃描與週期性
      研究報告。
    </li>
    <li>
      <strong>自我照護基礎設施</strong> —— gateway 每 30 分鐘自我修復、
      記憶每小時備份、凌晨 3 點 memory-promote、每月 Daily Log 歸檔。
    </li>
    <li><strong>以及，現在多了一個家庭行程提醒。</strong></li>
  </ul>

  <p>
    二十個 job、同一台機器、同一個排程系統、運維狀態統一推到一個 Telegram
    chat。把第 21 個（其實是第 20 個 —— 這次遷移是用本地的取代了 n8n 上的，
    總數沒變）加進來的邊際成本基本上是 0。
  </p>

  <h2>心得</h2>

  <Lesson label="關於成本紀律">
    $7.88/月 是那種不會觸發警報的金額 —— 三杯咖啡、一個串流訂閱。
    但紀律就是要持續問「這筆錢買的是什麼、我還需要它嗎？」
    一旦答案是「一個我下午就能重寫的 workflow」，決定其實已經做完了。
  </Lesson>

  <Lesson label="關於視覺化 workflow 與腳本">
    十個 n8n node 在 canvas 上看起來架構感很強，但程式碼其實沒多少。
    一旦出現 Code node —— 那個放諸四海皆通的「我放棄視覺抽象」訊號 ——
    workflow 真正的邏輯都已經搬出 graph 了。Graph 是包裝；
    包裝才是你在付錢的東西。
  </Lesson>

  <Lesson label="關於框架攤平">
    一個 cron job 撐不起一套排程系統，二十個就可以。最好的遷移時機是：
    你已經為了其他理由付過框架成本，然後手上還剩一個僅靠單一 workflow
    撐著的託管工具。
  </Lesson>
</BlogPost>
